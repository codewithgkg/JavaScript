// Install Git bash for backend

// In Mac OS it is pre install, however for window you need to install it manually

// Go to link https://gitforwindows.org/ and download and then set it up

// open VS code, View < Command palette > select default profile > bash

// Now your terminal will open with bash by default